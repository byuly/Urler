package com.urler.dto;

public class AffiliateCampaignDTO {
    // Only sends relevant fields (like campaign name, referral code, commission rate) instead of exposing internal DB fields or sensitive user info.
}
