package com.urler.controller;

public class EngagementController {
    // Dedicated to returning insights like click trends, heatmaps, CTR (Click-Through Rate),
    // etc. Keeps analytical logic out of core CRUD controllers.
}
