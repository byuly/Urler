package com.urler.controller;

public class AffiliateController {
    // Manages endpoints related to affiliate campaigns,
    // e.g., POST /affiliate, GET /affiliate/{id}, or report generation. Keeps concerns isolated from regular campaigns.
}
